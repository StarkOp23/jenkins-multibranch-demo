pipeline{
    agent any{
        stages{
            stage("build"){
                echo "Build is running..."
            }
        }
        stages{
            stage("test"){
                echo "Test is running..."
            }
        }
        stages{
            stage("deploy"){
                echo "Deploy is running..."
            }
        }
    }
}
